area - 4000

production = 2.41 * area
gross return = production * market_price
get per product cost from table 3
total production cost = area * per production cost
gross margin = gross return - total_production cost

---> next step ---->

ENMc = acpected net margin (every_crop - selected_crop_by_farmer)
if positive result then select as alternative crop otherwise  not.




